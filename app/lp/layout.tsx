// Standalone layout — no Navbar, no Footer, no ChatWidget, no ScrollToTop
// This page is for Meta ads traffic only. Zero distractions.
export default function LpLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
